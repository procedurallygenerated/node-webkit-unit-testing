var assert = require("assert");

describe("Sample test case", function(){
	it("Should not fail",function(){
		assert(true);
	});
	it("Keep editing the spec file and the test auto reload?",function(){
		assert(true);
	});
});